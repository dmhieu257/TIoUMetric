# TIoUMetric

Release on 20/07/2023. This repository is built for evaluation arbitrary scode.

Required to install python 2.7.18 

Install required module
  ```Shell
  pip install Polygon2
  ```
Then run
  ```Shell
  python script.py -g=gt.zip -s=det.zip
  ```
